{
  "name": "@airp/demo",
  "version": "0.1.0-pre",
  "description": "The scripted end-to-end scenario, plus the mock providers it runs against.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/mock-script.test.mjs"
  },
  "dependencies": {
    "@airp/core": "*",
    "@airp/store-sqlite": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
