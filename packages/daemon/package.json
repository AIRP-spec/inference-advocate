{
  "name": "@airp/daemon",
  "version": "0.1.0-pre",
  "description": "Local HostSession host: HTTP daemon on 127.0.0.1 for the browser tab, and loopback RPC for desktop.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/host.test.mjs"
  },
  "dependencies": {
    "@airp/core": "*",
    "@airp/store-sqlite": "*",
    "@airp/evaluator-local": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
